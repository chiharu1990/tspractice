export default {};
// ⑤ 処理を追加して、コメントの指示の値をコンソールに表示してください
const stageName = "Taeyong";
const birth = 1995;

// result が 以下の値になるように書いてください
// { stageName: "Taeyong", birth: 1995 }
const result = {
  stageName: stageName,
  birth: birth,
};

// 以下の行は変更せず、コンソールにコメントにある値を表示してください。
console.log(result) // => { stageName: "Taeyong", birth: 1995 }